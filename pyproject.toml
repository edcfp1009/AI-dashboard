[project]
name = "ai-dashboard"
version = "0.1.0"
description = "FreightPOP AI product metrics dashboard (MCP + Copilot + Accessorial Agent)"
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]
